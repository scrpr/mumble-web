# @mumble-web/web

Next.js（App Router）前端：连接网关、展示频道/用户/聊天（M0–M2 阶段不含语音）。

## 环境变量

- `NEXT_PUBLIC_GATEWAY_WS_URL`：网关 WebSocket 地址（默认 `ws://localhost:64737`）

## 启动（开发）

```bash
pnpm -C apps/web dev
```

